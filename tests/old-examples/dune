(alias
  (name runtest)
  (deps (:test old-examples.t) (glob_files *.sfw) (glob_files *.sfwi))
  (action
    (progn
      (setenv CERVOISE %{exe:../../src/main.exe}
        (setenv CERVOISE_LIBDIR %{project_root}/stdlib
          (run craml %{test})))
      (diff? %{test} %{test}.corrected))))
