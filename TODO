=== TODO ===

== Short term ==

* Clean:
  * Fix TODO
  * Remove names in backend (« %comment = call donothing » or metadata instead) ?
  * Add functions in backend (like access_env …)
  * Add more explicit type names (like « type name = string » instead of just use a string)

* Cleaning (parser, typechecker, backend, …)
* Unit-tests
* Pure exceptions
  * Separate effect Exn of the other effects (IO, (…?))
* Finish the module system


== Short/Medium term ==

* Handle dynamic exceptions (allocation failures, …)
    let main try with
      | CannotAllocate -> ()
    end =
      dosomething ()
* Records
* Improve FFI
  * Verify that the function given has the same arity
  * The function should have an effect IO « String -> String -[IO]-> Unit »
  * Dynamically create wrappers for arity > 1
* Dynamically create a wrapper when partially apply a function instead of creating a lambda for each arguments (needs an analysis)


== Long term ==

* Recursive type aliases
* Compile libraries
