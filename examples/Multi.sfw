type Int =
  | O
  | S Int

let id = \(x:Int->Int) -> x

let val = id (λ(x:Int) -> x)

let idprime = id

let main = Unit

class Show a =
  let show : a -[]-> Unit
end

let show = show

let main = Unit
