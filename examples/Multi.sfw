type Nat =
  | O
  | S Nat

let id = \(x:Nat->Nat) -> x

let val = id (λ(x:Nat) -> x)

let idprime = id

let main = Unit

class Show A =
  let show : A -[]-> Unit
end

let show = show

let main = Unit
